---
name: slide-forge
description: 幻灯片模板库与锻造机制：模板分内置、已吸收、项目本地三层，把一页 slide 的版式结构作为模板管理，可新建项目、新增/修改/删除一页、新增/修改/删除模板，遇到没见过的版式时自动在项目内 .slide-forge/ 锻造成新模板，并经用户确认后吸收进全局 skill。当用户提到"做 PPT""做 slides""加一页""改版式/配色/字号/间距""这页换个排法""吸收经验"时使用。
---

# slide-forge · 极简学术风 Slide 模板库

一套刻意收得很紧的 Beamer 模板。风格：**理性、干净、克制**——层级靠字重、位置、
留白表达，不靠装饰。

**slide-forge 是一个会自我进化的模板库。** 遇到没见过的版式，把它锻造成新模板，
写进**项目本地**的 `.slide-forge/`；用户认可后再吸收进全局 skill。
这件事对用户是透明的——**不要等用户提醒你更新模板**。

## 术语（先说清楚，免得看不懂）

**deck = 一份完整的幻灯片 = 一个 deck 项目。** 这三个说法在本文件里指的是
同一个东西，别被绕晕：

| 说法 | 指的是 |
|---|---|
| **一个 deck** | 一整套幻灯片，对应磁盘上一个 `<项目>/slide/` 目录 |
| **一页 slide** | deck 里的一页，对应一个 `pages/NN-name.tex` 文件 |
| **`<项目>`** | 装这个 deck 的工作目录，里面只有 `slide/` 是 skill 管的 |

**全篇只用一个词指它：deck。** 不要再写"一个 deck 项目"这种叠词。
需要指磁盘位置时写 `<项目>/slide/`，需要指那份演示文稿时写 deck。

**`\Deck*` 里的 `Deck` 只是命名前缀**（页面级命令用 `\Deck*`、
字号类用 `\Slide*`），与上面这个概念无关，不要从命令名去推断结构。

## 核心流程（先拿模板，再改项目）

**每一页都走这一条，新建和修改都一样。顺序不能反。**

```
① 先看有没有现成的：查 base（内置）→ evolved（仓库）→ .slide-forge（本 deck）
   ├─ 命中一个 ──→ 直接用它，报一句"用 T4 排"
   ├─ 命中多个 ──→ 问用户，把候选摆出来让他选
   └─ 都没有 ────→ ②
② 在 .slide-forge/ 里新建模板（唯一"造模板"的地方，不用问授权）
③ 模板拿到手之后，才动这一页的内容
```

**"先拿模板，再改项目"** = 即使是"把这页改一下"这种要求，也不是直接改页面
文件；先判断它属于哪种版式 → 走①~③ → 拿到模板 → 再填内容。

## 三个位置，三种权限（本 skill 的全部规则都从这里推出来）

| | 是什么 | 谁能改 | 怎么进编译 | 进 git |
|---|---|---|---|---|
| **`base/`** | **内置**：跨人共享的基线 | ❌ **任何情况下 SKILL 都不改它**。只有用户明确要求才动 | `style.tex` 自带全部内容 | 是 |
| **`evolved/`** | **仓库**：吸收来的模板 | ⚠️ **只有用户明确说"吸收"，或你让他选、他同意时**才写 | ❌ **不参与编译**，要显式复制进 `.slide-forge/` | 否 |
| **`.slide-forge/`** | **自动**：本 deck 的锻造台 | ✅ 自动写，不用问授权 | `style.tex` 末尾两行 `\IfFileExists` | 否 |

**为什么 `.slide-forge/` 是自动的**：它落在用户的工作目录里，写它不需要任何
额外授权。所以"发现新版式 → 立刻固化成模板"这一步可以**直接做**，
不用中断去问"我要写工作区外面的文件夹，可以吗"。这是这个设计存在的理由。

**为什么 `evolved/` 必须用户点头才能写**：它是跨 deck 共享的仓库。
一旦写进去，别的 deck 就有可能取用它 —— 那是别人的项目，得他同意。

**为什么 `base/` 不许改**：它是所有人共享的基线。想改内置模板的排法，
不要在 `base/` 里改 —— 在 `.slide-forge/` 里写一份同名定义覆盖它。

---

## 第一部分：三层模板（先读懂这一节，其余都是它的推论）

同一份 skill，模板有三个来源。**查的顺序是 base → evolved → .slide-forge**，
但**编译时只有两层**（内置 + 本 deck 的 `.slide-forge/`）：

| 层 | 住在哪 | SKILL 能写吗 | 谁能用 | 进 git 吗 |
|---|---|---|---|---|
| **内置 base** | `~/.dsh/skills/slide-forge/base/` | ❌ **不改**（只有用户明确要求才动） | 所有 deck、所有人 | 是 |
| **仓库 evolved** | `~/.dsh/skills/slide-forge/evolved/` | ⚠️ 只在用户明确说"吸收"或他选同意时 | 所有 deck（需显式取用） | 否 |
| **自动 .slide-forge** | `<项目>/slide/.slide-forge/` | ✅ 自动写，不用问授权 | **只有这个 deck** | 否 |

### 为什么要这么分

`.slide-forge/` 落在**用户的工作目录里**，写它不需要任何额外授权。
所以"发现新版式 → 立刻固化"这一步可以**直接做**，不用中断去问
"我要写工作区外面的文件夹，可以吗"。要跨 deck 复用时，才由用户决定吸收。

**三条推论，每条都别搞反：**

1. **自动进化 = 自动往 `.slide-forge/` 里生成模板。** 发现新版式 → 先在
   `.slide-forge/templates/components.tex` 里生成，再在 deck 里用它。
   **改也改在 `.slide-forge/` 里**（那是锻造台），不要去改别处。
   做完告诉用户一句，不要为此打断他。
2. **`evolved/` 是仓库，不是编译层。** 它**不参与任何 deck 的编译** ——
   `style.tex` 里只加载 `.slide-forge/`。要用仓库里的模板就把那份
   **复制**进 `.slide-forge/templates/`。
   —— 这一条是踩过坑才定的：曾把 `evolved/` 垫在每份 deck 底下自动加载，
   结果任何一次吸收都会**静默改掉所有已有 deck 的排版**（实测零错误零警告，
   版式却变了）。已交付的 deck 必须冻结，所以每份 deck 只认自己那份。
3. **吸收是逐字复制，不手改。** `evolved/` 里那份必须与
   `.slide-forge/` 里那份**内容完全一致**（连注释都一样，只加一行来源标注）。
   两个目录结构相同、内容相同，只是位置不同 —— 这样"同名不同内容"的
   冲突根本不会出现。一旦吸收时顺手改了内容，两份就分叉了，那才是错误来源。

### 三个目录：结构完全一致，只区分来源

`evolved/` 与 `<项目>/slide/.slide-forge/` 的**结构与 `base/` 一致**，这是刻意的
—— 一眼就能看出哪个文件和哪个文件是同一类，区别只在来源。
**那两个目录的内容也应当一致**（吸收就是逐字复制），只是位置不同：

```
base/                              evolved/                       .slide-forge/（deck 里）
├── templates/            ←——→     ├── templates/        ←——→    ├── templates/
│   ├── format.tex                   │   ├── format.tex             │   ├── format.tex
│   ├── components.tex               │   └── components.tex         │   └── components.tex
│   ├── style.tex                    │                              │
│   ├── main.tex                     └── pages-examples/    ←——→    └── pages-examples/
│   └── pages/                           └── tN-<name>.tex             └── tN-<name>.tex
└── pages-examples/
    └── t1..t7 参考文件
```

**只对应两个子树**：`templates/` 与 `pages-examples/`。三层用同一套文件名与
同一套子目录名，**不要新造第三种目录** —— 结构一旦对不上，就又要靠记忆去分辨
"这文件是干嘛的"。

`base/templates/` 里的 `style.tex`、`main.tex`、`pages/`
在后两层没有对应物：它们是**每次新建 deck 就整份实例化**的脚手架，没有"吸收"
这一说（每个 deck 都必须有自己的那一份）。只有 `format.tex` 与 `components.tex`
会积累跨 deck 的经验。**对应的是子树与角色，不是逐个文件。**

### ⚠ `evolved/` 与 `.slide-forge/` 随时可能被删空 —— 里面不许放机制说明

这两个目录是**可丢弃的**：用户随时可以清空、删除、重建。所以：

- **里面只放真正的定义**：组件、格式覆盖、参考文件
- **不要放 README、不要写"本目录是干嘛的""怎么用这个 skill"** ——
  一旦被删空，那些说明就白写了；而且它们会让人以为"这目录里有东西"
- 机制、约定、命名规则**全部写在 `SKILL.md` 与 `references/` 里**
  （只有这两个地方是稳定、必须存在的）
- **没有内容时不要建文件**：空骨架（只有注释）会让"这层到底有没有东西"
  变得难判断。文件在第一次有内容时才出现；加载器用的是 `\IfFileExists`，
  少一个文件不会有任何影响

### 编译加载链（决定放哪儿才算数）

```latex
% <项目>/slide/main.tex
\input{style.tex}     % 项目快照 = base/templates/style.tex 的副本，
                      % 它内部按顺序加载：
                      %   1. 内置基线（就在这一份文件里）
                      %   2. .slide-forge/templates/format.tex      存在才加载
                      %      .slide-forge/templates/components.tex  存在才加载
```

**只有两层**（内置 → 项目本地），后加载的覆盖先加载的。
`evolved/` **不在这条链上** —— 加了它，任何一次吸收都会静默改变所有已有
deck 的排版。要用仓库里的模板，就把它**复制**进 `.slide-forge/templates/`。

**这样每份 deck 的排版只由「基线 + 自己那份 `.slide-forge/`」决定**，
别人吸收什么、改什么都不影响它。代价是取用时要复制一次，这个代价是值得付的。

**在进化层里定义组件一律用 `\DeckDefine`，不要用 `\newcommand`** ——
`\newcommand` 遇到已存在的命令会直接报错（`Command \DeckX already defined`），
覆盖就无从谈起；`\renewcommand` 又要求命令已存在。`\DeckDefine` 两种都吃。
细节见 `base/templates/style.tex` 第七节。

`\IfFileExists` 让本地层是**可选**的：全新 deck、或者 `.slide-forge/` 被整个
删掉，都能照常编译。**cwd 必须是 `<项目>/slide/`** —— 那两行是相对路径，
换个目录连 `\input{style.tex}` 都会失败（`File 'style.tex' not found`）。
详见 `references/typography.md` 第七节。

---

## 第二部分：核心纪律（不可协商）

改任何东西之前先读这一节。**违反其中任何一条，风格就散了。**

1. **字号只有两档**
   - 页标题 `\SlideTitleFont` = 20pt / 行距 26pt
   - 正文 `\SlideTextFont` = 12pt / 行距 17pt
   - 唯一例外：封面与致谢页主标题 `\SlideCoverFont` = 26pt / 32pt
   - 正文页出现任何 `\fontsize` 都算破坏约束。要强调 → `\textbf` 加粗。

2. **行距只有两种**，由字号命令的第二个参数控制，不另设 `\linespread`。
   表格行距用 `\DeckTableLeading` 按页覆盖。

3. **正文一律黑色**（`#14161A`），页标题用强调色（`#26308F`）。
   **全篇不用斜体**——中文字体没有真斜体字形，合成倾斜会发虚。

---

## 第三部分：模板注册表 ← 本 skill 的记忆

**模板 = 一页的版式结构。** 某页的具体内容只是对它占位符的填充。
一页只可能属于一种模板。

**本表只登记"内置"与"已吸收"两层。** 项目本地 `.slide-forge/` 里的模板不进本表
—— 它们只服务一个项目，登记在这里会污染所有项目的候选列表；而且
`.slide-forge/` 随时可能被删空，登记了也是过期数据。
**项目本地模板在哪登记？不登记。** 要用时直接读那个项目的
`.slide-forge/templates/components.tex` 与 `pages-examples/`，现读现用。
只有用户确认吸收之后，才在本表加一行。

### 使用中的模板

| ID | 层 | 名称 | 结构 | 组件 | 参考文件 |
|---|---|---|---|---|---|
| T1 | 内置 | 纯文字分组 | 若干行文字，用留白分组 | `\DeckLine` `\DeckGap` | `base/pages-examples/t1-plain.tex` |
| T2 | 内置 | 时间标签条目 | 短标签 + 内容，标签左对齐成列 | `\DeckTagged` | `base/pages-examples/t2-tagged.tex` |
| T3 | 内置 | 名称—数值表 | 表格：长名称占满 + 数值右对齐 | `\DeckCourseTable` `\DeckCourseHead` `\DeckCourseRule` | `base/pages-examples/t3-table-value.tex` |
| T4 | 内置 | 标签—长文字表 | 表格：窄标签左 + 长文字占满左对齐 | `\DeckListTable` `\DeckListHead` `\DeckCourseRule` | `base/pages-examples/t4-table-list.tex` |
| T5 | 内置 | 作品/论文条目 | 一条两行（标题行 + 说明行），可内嵌链接 | `\DeckCite` `\DeckLink` | `base/pages-examples/t5-cite.tex` |
| T6 | 内置 | 两图并排 | 两张等高图，顶端对齐，居中 | `\DeckFigures` | `base/pages-examples/t6-figures.tex` |
| T7 | 内置 | 无标题页 | 无页标题，内容垂直居中 | `\DeckBare` | `base/pages-examples/t7-bare.tex` |

**目前没有"已吸收"层的模板**（`evolved/` 是空的）。某个项目沉淀出新模板、
用户确认吸收之后，在这里加一行，层填「已吸收」。

**封面只有内置 `pages/00-cover.tex`（主标题 + 姓名 + 单位）这一种**，
用于个人介绍、求职这类需要署名的 deck。不需要署名的封面
（内部技术汇报、组会）曾有一个 T8 `\DeckCoverPlain`，后来连同 `evolved/`
一起清掉了 —— 需要时由某个项目在 `.slide-forge/` 里重新沉淀，再走吸收。

---

## 第四部分：每一页的固定流程（先拿模板，再改项目）

**这是本 skill 最重要的一条流程。新建一页、改一页，都走它。**

```
① 先看有没有现成的
   查三个位置：base（内置）→ evolved（仓库）→ .slide-forge（本 deck）

   ├─ 命中一个 ────────→ 直接用它，报一句"用 T4 排"
   ├─ 命中多个 ────────→ **问用户**，把候选摆出来让他选
   └─ 一个都没命中 ────→ ②

② 没有现成的 → 在 .slide-forge/ 里新建 → 拿到了
   （这是唯一"造模板"的地方，见第五部分。不需要问用户授权）

③ 模板拿到手之后，才动页面内容
   —— **先拿模板，再改项目。顺序不能反。**
```

**"先拿模板再改项目"意味着什么**：即使你的任务是"把这页改一下"，也不是直接
上手改页面文件。先判断这个新要求属于哪种版式 → 走上面①~③ → 拿到模板 →
再往里填内容。**用户提出修改时，同样是这套流程**（可能选到现成的，
也可能需要新建一个）。

**改也是改模板，不是改页面**：要调排版就回 `.slide-forge/templates/` 改
组件定义，页面文件里永远只有"组件名 + 纯文本"。页面里出现 `\fontsize` /
`\vspace` / `\setbeamercolor` 就是设计债。

### 什么时候直接选、什么时候问

- **直接选**（智能选择）：只有一个结构上说得通的候选。选完**报一句**用了哪个
  （"这一页用 T4 标签—长文字表排"），让用户有机会拦
- **必须问**：命中多个候选，或用户描述含糊分不清。问法要具体，把候选摆出来：

> 这一页可以有三种排法：
> ① **T4 标签—长文字表**（内置）：年份在左、描述在右，适合 5–8 条；
> ② **T9 时间轴**（仓库里，已吸收）：竖向轴 + 节点标注；
> ③ **T12 三栏对比**（本项目 `.slide-forge/`）：三列文字，每列一个标题。
> 你要哪种？

### 三层怎么查、怎么"拿到"

| 层 | 怎么查 | 怎么拿到 |
|---|---|---|
| **base** 内置 | 第三部分的注册表 | **本来就在**（`style.tex` 自带全部组件），直接用 |
| **evolved** 仓库 | 读 `evolved/templates/components.tex` + 第三部分注册表 | **复制**一份进 `.slide-forge/templates/`（逐字复制，不改内容） |
| **.slide-forge** 本 deck | 读 `.slide-forge/templates/components.tex` | 本来就在，直接用 |

- **`evolved/` 不在编译链上**，所以从它取用必须复制一次 —— 这是刻意的：
  详见第一部分推论 2
- **遇到重名**：本 deck 的 `.slide-forge/` 那份优先（它更贴近这个 deck 的实际）
- 用户选完之后，**在页面文件的头注释里记下用了哪个模板**，
  下次改这页时不必重新推断

---

## 第五部分：模板的进化（本 skill 的核心机制）

### 触发条件

出现以下任一情况时，**必须**把新版式固化成模板：

- 页面的结构无法用现有模板表达（需要新的组件、新的分栏方式、新的对齐逻辑）
- 现有模板需要被扩展才能容纳这页（例如加了新的参数）
- 用户明确说"这页换个排法""做成时间线的样子"

### 进化流程（每次都走完）

1. **判断是"新模板"还是"扩展已有模板"**
   - 版式结构本质不同 → 新模板
   - 只是同一结构多了个参数 → 扩展已有模板（改组件签名，更新注册表）

2. **在 `<项目>/slide/.slide-forge/templates/components.tex` 里实现组件**
   - **这一步直接做，不要问用户要授权** —— 它在工作目录内，没有理由打断他
   - 目录/文件不存在就建（`templates/` 这一层必须有，要与 `evolved/` 对齐）
   - 用 **`\DeckDefine`** 定义组件，**不要用 `\newcommand`**
   - **不要改 `base/`** —— 那是跨人共享的基线，改了会污染 git 追踪的内容
   - **不要写全局 `evolved/`** —— 那是吸收的落点，不是锻造的落点
   - 新组件命名沿用现有前缀：页面级用 `\Deck*`，字号/尺寸用 `\Slide*`
   - 组件定义上方写注释：用途、参数、为什么这么做、有什么坑
   - **绝不在页面文件里写裸样式**——那不算模板
   - 改**格式配置**（颜色 / 字号 / 间距）则写进
     `.slide-forge/templates/format.tex`（按"基线 → 本地"的顺序加载）

3. **建参考文件 `.slide-forge/pages-examples/tN-<name>.tex`**
   - 内容用中性占位符（"第一条""内容写在标签右侧"），不要用具体项目的内容
   - 头部注释里写清：适用场景、用法要点、容量上限、易错点

4. **这个 deck 自己先用起来，并且改也只改这里**
   - **演进顺序是固定的：先在 `.slide-forge/` 生成 → 再在项目里用 → 不满意就
     回来改 `.slide-forge/` 里那一份。** 不要在别处改（尤其不要去改 `base/`
     或 `evolved/`）—— 那会让这个 deck 看到的定义和锻造台上的那份不一致
   - 改完重新编译目视

5. **登记：只在用户同意吸收之后才写注册表**
   - 项目本地模板**不进** `SKILL.md` 的注册表 —— 它只服务一个 deck，
     进了表会污染别的 deck 的候选列表（理由见第三部分）
   - 所以这一步什么都不用做。**`.slide-forge/` 里不要建登记表文件** ——
     那个目录随时可能被删空（见第一部分）
   - 等到吸收时，才在 `SKILL.md` 注册表加一行

6. **编译并目视这一页**（见第八部分）

7. **告诉用户一句，并提吸收**
   - "这页的时间轴排法现有模板装不下，我已经在 `.slide-forge/` 里
     锻造成 T9 了（组件 `\DeckTimeline`）。要吸收进全局仓库、让别的
     deck 也能用吗？"
   - 用户说不要 → 到此为止，**不再追问**。
     但**下次别的东西再复用到 T9 时，主动再提一次** —— 复用是它值得吸收的证据。

### 不要做的事

- ❌ 让用户提醒你更新模板 —— **这是你的责任**
- ❌ 把一次性的排版当模板记下来（只在一页用、不会有第二次的，不建模板）
- ❌ 在 `.slide-forge/` 之外的地方改这个 deck 的排版（定义只该有一个出处）
- ❌ 为了"省事"把新组件直接写进全局 `evolved/`，绕开用户的选择权

---

## 第六部分：吸收经验（把项目本地提升为全局）

### 什么时候提

- 刚在项目本地沉淀完一个模板时（一次就好，问过就不再问）
- **复用节点**：某个模板被第二个 deck、或同一个 deck 里的第二页用到时，
  主动提 —— 这是它已经证明自己有第二次的硬证据
- 用户自己说"吸收""把这个存进全局""以后别的 deck 也要用"

### 怎么执行

1. **列出可吸收项**（读 `<项目>/slide/.slide-forge/` 下的三处）
   - `templates/components.tex` 里的每个 `\Deck*` / `\Slide*` 组件
   - `templates/format.tex` 里的每个覆盖项
   - `pages-examples/*.tex`
   - 把清单摊给用户挑，不要默认全收

2. **先做通用性检查**（吸收前唯一的关卡，别跳）
   - 内容里有没有**具体项目的东西**：产品名、专属数据、写死的年份/人名
   - 有没有 `\input` / `\includegraphics` 指向这个 deck 本地的文件
   - 是不是只在**这一页**用过、看不出会有第二次
   - 命中任何一条 → 报告给用户，建议不吸收。**吸收了别人也用不了，
     还会让仓库里多一份只对这个 deck 成立的"假通用"模板**

3. **检查与全局 `evolved/templates/` 的冲突**
   - 同名组件已存在且内容不同 → 报告差异，问用户：**是同一模板的新版本吗？**
     —— 是则整体替换那一份（并说清旧版本长什么样）；不是则另起名字
   - 同名且内容相同 → 跳过，说一声"已经在仓库里了"
   - 绝不在有差异时静默覆盖，也绝不做"合并"（两份混成一份必错）

4. **逐字复制到全局 `~/.dsh/skills/slide-forge/evolved/`**
   —— 位置与 `base/` 对应，内容与 `.slide-forge/` 里那份**完全一致**：
   - 组件 → `evolved/templates/components.tex`（用 `\DeckDefine`）
   - 格式覆盖 → `evolved/templates/format.tex`
   - 参考文件 → `evolved/pages-examples/`（原样复制）
   - **唯一的改动是加一行来源标注**：`% 吸收自 <deck 名>，<日期>`
   - ❌ **不要顺手改内容、不要"优化一下"、不要改参数** ——
     吸收是**搬运**，不是编辑。内容一改，仓库那份就和锻造台上的分叉了，
     下次别人取用时你无法判断哪份是对的
   - 文件不存在就新建；**只放定义，不要写说明文字**（那个目录随时可能被删空）

5. **验证两份一致**（吸收的验收标准）
   - 组件：两边的 `\Deck*` 定义逐字相同（只差那行来源标注）
   - 参考文件：两份文件内容完全一样
   - 不一致就回去改，别交付

6. **更新本文件第三部分的注册表**：加一行，层填「已吸收」

7. **项目本地那份一字不动。** 它还在被这个 deck 使用，而且它就是这个
   deck 的冻结版本 —— 刚才那一步是复制，不是搬家。

8. **提议升入基线**：如果这个模板通用性很高、不涉及个人偏好，
   可以问一句"要不要把它也放进 `base/`，让干净的 skill 装完就有？"
   —— 同样只提一句，用户说了才动 `base/`。

### 吸收之后

- 复述一遍做了什么（写进了哪些文件、注册表加了哪几行）
- **不必重编译任何 deck**：吸收只是往仓库里放了一份，没有 deck 会因此改变
  —— `evolved/` 不在编译链上，这是它的设计前提
- 如果有哪个 deck **想**用这份新版：把仓库那份复制进它的
  `.slide-forge/templates/`，然后重编译目视

---

## 第七部分：经验与 Skill 管理（用户可随意操作）

用户说"这个模板不要了""把 T9 改一下""清理一下 `.slide-forge`"时，按这里做。

### 用户可以自由增删改的，只有这两处

- `<项目>/slide/.slide-forge/` 里的任何东西
- `~/.dsh/skills/slide-forge/evolved/` 里的任何东西

移动、合并、改名、删除都行。**用户改了之后，本文件的注册表要跟着对齐**
（他删掉一个已吸收模板，就把那一行也删掉）。

### 其余一律不许改

`base/`、`SKILL.md`、`references/`：**除下面两个例外，任何操作都不写这些文件。**

1. **吸收**会改 `SKILL.md` 的注册表（第六部分第 4 步）—— 这是登记的必然结果
2. 用户明确要求改 `base/` 时

想改一个内置模板的排法时，**正确做法是在 `.slide-forge/` 里做一个本地变体**：

```latex
% .slide-forge/templates/components.tex
% T4 的本地变体：标签列加宽到 8em（原版 5.6em），因为这个项目的年份标签长
\renewcommand{\DeckListTable}[1]{ ... }
```

`\renewcommand` 覆盖基线，基线本身一个字没动。将来若要分享，
吸收时按"本地变体"进全局。

### 修改格式配置（用户说"换个颜色""字号大一点""表格再紧一点"）

1. 先改**当前项目**的 `.slide-forge/templates/format.tex` —— 改完立刻编译目视
2. 用户满意 → 提一句"要不要吸收进全局，以后所有 deck 都用这套？"
3. 吸收的完整清单见 `references/absorption.md` 第四节
4. 全局默认值一改，**所有还在动的项目都得重编译目视** ——
   行距类改动会连锁影响每一页的行数，原来刚好放满的页会溢出

**判断顺序**：先动留白 → 再动颜色 → 最后才考虑字号。字号是最后手段，
因为它是唯一能让整页节奏崩掉的东西。

### 删除模板

1. 确定它在哪一层（内置 / 已吸收 / 项目本地）
2. **先查还有哪些页面在用它** —— 搜组件名，别只看注册表
3. 有页面在用 → 先问用户这些页改成哪种排法，**改完页面再删模板**；
   直接删会让项目编译不过
4. 删掉定义（本地变体删 `.slide-forge/templates/components.tex` 里那份；
   已吸收删 `evolved/templates/components.tex` 里那份）与参考文件
   - 某一层删空之后，那个文件如果只剩注释，**把文件也删掉** ——
     空骨架会让"这层到底有没有东西"变得难判断
5. 删掉登记行：已吸收的删本文件第三部分那行（项目本地的本来就没登记）
6. **内置模板不删** —— 那不属于用户的经验，属于共享基线。
   用户说"以后别用这种排法"时，把它从候选里划掉即可，
   或在 `.slide-forge/templates/components.tex` 里用一个同名 `\DeckDefine`
   把它改成他要的样子

---

## 第八部分：必须目视（不要只看编译是否报错）

beamer 的 frame 高度是硬的：**超出部分被静默裁掉，编译只有一条 overfull 警告，
有时连警告都没有。** 所以新增或修改任何一页后都要：

1. 编译，确认零错误、零 overfull、零字体警告；
2. 渲染成图**看一眼**，确认最后一行还在。

```powershell
# cwd 必须是 <项目>/slide/
Remove-Item main.log -Force -ErrorAction SilentlyContinue   # ← 见下方警告
xelatex -interaction=nonstopmode main.tex
Select-String -Path main.log -Pattern "^! |Overfull|Font Warning"
pdftocairo -png -r 130 -f <页码> -l <页码> main.pdf out
```

⚠ **读日志前必须先删日志。** 失败的 xelatex 常常根本不写 `main.log`，
上一次成功那份会原样留着 —— 你会读到过期证据，得出"编译成功"的相反结论。
这个坑在写验证脚本时实地踩到过一次，细节见 `references/typography.md` 第七节。

### 排版实测数据

- 正文 12pt、宽 14.25cm 时，**一行约 30 个全角字符**（西文数字约占半角）。
  超过必然折行，折行点不受控，常把末尾两三个字甩到下一行——很难看。
- **一页正文最多 6 行**（`\DeckLine` 这一级）。第 7 行起溢出被裁。
- 表格：T3 最多 5 行数据 + 表头；T4 最多 8 行。
- T6 图片：两图宽度之和**必须明显小于 14.25cm**，留 0.3cm 以上。
  只差 0.03cm 就会掉行并报 overfull vbox（报的是竖直溢出，极难查）。

细节与更多陷阱见 `references/typography.md`；
吸收操作的完整清单见 `references/absorption.md`。

---

## 第九部分：当前配置

改配置后必须同步这一节。

| 项 | 值 |
|---|---|
| 页面 | 16:9，`aspectratio=169`，11pt base |
| 页边距 | 左右各 0.90cm |
| 背景 | `#FBFAF7` 暖白 |
| 正文色 | `#14161A` 近黑 |
| 强调色 | `#26308F` 低饱和深靛蓝（页标题 / 链接 / 封面主标题） |
| 中文字体 | Microsoft YaHei（微软雅黑） |
| 西文字体 | Segoe UI |
| 字号 | 页标题 20/26、正文 12/17、封面主标题 26/32 |
| 页标题位置 | 左上角，`0.50cm` 上边距，无分隔线无页脚 |
| 正文起始位移 | `SlideBodyTopSkip` = 0.40cm |
| 行间距（同组） | `\DeckLine` 条目间 0.30cm |
| 块间距（换组） | `\DeckGap` = 0.40cm |
| 列表符号 | 无（悬挂缩进，折行与首行文字对齐） |
| 表格 | 无竖线无外框，仅表头下一条 0.4pt 细线 |

**上表是全局默认值。** 单个 decks 若在 `.slide-forge/templates/format.tex` 里覆盖了某一项，
以那个项目的实际编译结果为准；吸收之后才回写到本表。

---

## 第十部分：文件布局（哪份文件是谁的，别搞混）

### 全局 skill（`~/.dsh/skills/slide-forge/`）

```
~/.dsh/skills/slide-forge/          也是一个 git 仓库
├── SKILL.md                         本文件（机制 + 注册表）        [追踪]
├── .gitignore                       追踪边界                      [追踪]
├── references/
│   ├── typography.md                排版实测数据与陷阱            [追踪]
│   └── absorption.md                吸收操作的执行清单            [追踪]
├── base/                            内置基线：跨人共享，**SKILL 不改** [追踪]
│   ├── templates/
│   │   ├── style.tex                ★ 唯一事实来源：基线 + 全部组件 + 两行加载器
│   │   │                              （新建 deck 复制这一份就够编译）
│   │   ├── main.tex                 页面清单
│   │   ├── format.tex               同内容副本（分层查阅用，**不复制进 deck**）
│   │   ├── components.tex           同内容副本（同上）
│   │   └── pages/00-cover.tex、10-thanks.tex
│   └── pages-examples/t1..t7        每种内置模板一个参考文件
└── evolved/                         吸收来的仓库：结构与 base/ 对应  [忽略]
    ├── templates/                   ← 对应 base/templates/
    │   ├── format.tex               吸收来的格式覆盖（有内容时才存在）
    │   └── components.tex           吸收来的组件（有内容时才存在，用 \DeckDefine）
    └── pages-examples/              ← 对应 base/pages-examples/
        └── tN-<name>.tex            吸收来的参考文件
```

**仓库里只放"别人拿到就能直接用"的东西。** 所以 `evolved/` 与 `_verify/`
都不追踪（见 `.gitignore`）：前者是本机专属的经验，后者是维护 skill 用的
验证脚本，别人 clone 下来都不需要。

**`evolved/` 里不放说明文件**（README 之类）—— 它随时可能被删空，
说明写在 `SKILL.md` 与 `references/` 里。两个空目录壳本身无害，
加载器用 `\IfFileExists`，少文件不会有任何影响。

### deck（`<工作目录>/`）

```
<工作目录>/
└── slide/                           出稿目录
    ├── main.tex                     页面清单
    ├── style.tex                    基线快照 + 末尾两行本地层加载
    ├── pages/                       一页一个文件
    ├── figures/
    ├── main.pdf                     交付物
    └── .slide-forge/                **本项目本地层**，随时可删
        ├── templates/               ← 与 evolved/templates/、base/templates/ 同名
        │   ├── components.tex       本 deck 的组件（锻造的 + 从仓库取用的）
        │   └── format.tex           本 deck 的格式覆盖（有覆盖项时才存在）
        └── pages-examples/          ← 与 evolved/pages-examples/ 同名
            └── tN-<name>.tex        新模板的参考文件
```

**deck 的目录里没有 `.gitignore`。** 这套流程是以模板为中心的：先有模板、
再有内容，操作的对象是模板文件本身。deck 要不要纳入版本控制，由用户自己的
工作流决定 —— skill 不生成、也不建议，不去替他做这个决定。

**`.slide-forge/` 里也不放说明文件或登记表** —— 同上，它随时可能被删空。

### 三条最容易搞混的边界

1. **`evolved/`（全局）与 `.slide-forge/`（项目）不是一个东西。**
   前者是"吸收过的、所有项目可用的图书馆"，后者是"这个项目自己长出来的"。
   两者都不进 git，但原因不同：前者是人各一份，后者是本机专属。
2. **`base/templates/format.tex` 与 `components.tex` 不复制进项目。**
   它们和 `.slide-forge/` 下的同名文件会打架 —— 两个同名文件都得改，
   迟早改分叉。项目只拿 `style.tex`（内容等价，唯一事实来源）。
3. **`\input` 的相对路径按 cwd 解析。** 编译时 cwd 必须是 `<项目>/slide/`，
   否则连 `style.tex` 都找不到。详见 `references/typography.md` 第七节。

---

## 第十一部分：三个日常功能

### 新建 deck（New）

用户说"新建一个 PPT / 做一份 slides"，目标目录为空或不含本模板时：

1. 建目录骨架，并复制全局 skill 的 `base/templates/` 里这**三样**：
   ```
   <目录>/slide/
   ├── main.tex                ← 页面清单
   ├── style.tex               ← 样式快照（末尾两行加载本 deck 的 .slide-forge/）
   ├── pages/                  ← 00-cover.tex、10-thanks.tex
   ├── figures/                ← 空目录
   └── .slide-forge/           ← **不建** —— 有内容时再出现
   ```
   **不要把 `base/templates/format.tex`、`components.tex` 复制进 deck** ——
   它们和 `.slide-forge/templates/` 下的同名文件会打架。
   复制过去的这份 `style.tex` 自带**完整的内置基线**，所以 deck 即使和 skill
   目录分开也能编译出正确的排版；它末尾那两行 `\IfFileExists` 只加载本 deck 的
   `.slide-forge/`（`evolved/` 不参与编译）。
2. 问用户：姓名、单位/身份、这份 deck 的用途（决定封面主标题怎么写）
3. 填封面（把 `姓名`/`单位　专业　身份` 占位符换掉）
4. 正文页按用户给的内容逐页建（用下面的"新增一页"，它本身就走核心流程）
5. **不要预先搬任何模板进来** —— 用到哪一页再按核心流程取。
   新建的 deck 一开始 `.slide-forge/` 是空的，这是正常的
6. **不生成 `.gitignore`**，也不建议用户给 deck 上 git ——
   那是他工作流的事，skill 不替他决定
7. 编译验证：零错误、零 overfull、零字体警告

不要问用户"用什么配色/字号"——格式已定，直接生成。

### 新增一页（Add page）

1. **走第四部分的固定流程**：先查三个位置有没有现成的
   （base → evolved → .slide-forge），命中一个就直接用、命中多个就问用户
2. **命中 base（内置）** → 什么都不用拿，`style.tex` 自带全部组件，直接写页面
3. **命中 evolved（仓库）** → **先把它取到这个 deck 里**，再动页面：
   - 把 `~/.dsh/skills/slide-forge/evolved/templates/components.tex` 里
     那个组件的定义（连同 `pages-examples/` 里的参考文件）**复制**到
     `<项目>/slide/.slide-forge/templates/`
   - **逐字复制，不要改**；文件头保留原有的来源标注
   - 取过一次就够了，之后这个 deck 用的是自己那份
   - 为什么要复制而不是自动加载：`evolved/` 不在编译链上。
     一旦让它垫在每份 deck 底下，任何一次吸收都会**静默改掉所有已有 deck**
     （实测零错误零警告，版式却变了）。每份 deck 只认自己那份，才冻结得住
4. **一个都没命中** → 回第五部分，在 `.slide-forge/` 里新建模板，然后
   才动页面（**"先拿模板，再改项目"**）
5. `main.tex` 加一行，文件名与顺序号对应：
   ```latex
   \DeckSlide{页面标题}{pages/NN-name.tex}
   ```
   **一个文件 = 一页 = `main.tex` 里一行。**
6. 写页面文件：只允许组件名 + 纯文本。禁止 `\fontsize` / `\vspace` /
   `\setbeamercolor`——出现即设计债，应回 `.slide-forge/templates/components.tex` 加组件
7. 编译并**渲染成图目视**（见第八部分）

### 修改格式配置（Modify）

见第七部分"修改格式配置"一节（含吸收到仓库的完整清单）。

---

## 第十二部分：反模式（明确不要做）

- ❌ 在页面文件里写 `\fontsize` / `\vspace` / `\setbeamercolor`
- ❌ 为「姓名」「要点」各建一个命令——两个命令意味着两套样式，迟早不一致
- ❌ 用圆点/方块做列表符号（12pt 下发灰、与基线打架）
- ❌ 用分隔线、小标题、色块分组——**用留白**
- ❌ 用 `\emph` / `\textit`（中文没有真斜体）
- ❌ 一页塞两组无关内容来"省页数"
- ❌ 只看编译通过就交付，不渲染成图目视
- ❌ 读 `main.log` 前不删旧日志（会读到过期证据，结论完全反过来）
- ❌ **遇到新版式却不固化成模板**（这是本 skill 存在的意义）
- ❌ **为了省事把新组件直接写进全局 `evolved/`**，绕开用户的吸收选择权
- ❌ 直接把 `.slide-forge/` 里那份"搬"进 `evolved/`（那是复制，不是搬家；
   deck 的页面还在用它）
- ❌ **吸收时顺手改内容**（吸收是逐字搬运。一改，仓库那份就和锻造台分叉了）
- ❌ **把 `evolved/` 写进 `style.tex` 的加载链**（那会让吸收静默改掉所有
  已有 deck 的排版，实测过）
- ❌ 改 `base/`——想改内置模板的排法，就在 `.slide-forge/` 里做本地变体
