---
name: slide-forge
description: 幻灯片模板库与锻造机制。记忆（模板与风格）与实际内容严格分离：从三处记忆里选一套模板拼装每一页，只填内容。遇到没有的版式就在本地记忆里锻造新模板，用户同意后可吸收到全局仓库供所有 deck 复用。当用户提到"做 PPT""做 slides""加一页""改版式/配色/字号""这页换个排法""换风格""吸收经验"时使用。
---

# slide-forge · 极简学术风 Slide 模板库

**本文件只回答"怎么做 PPT"，不描述"PPT 长什么样"。**

后者全部住在**记忆**里——模板、风格、容量。想知道字号是多少、一行能放几个字、
有哪些版式可选，去读记忆，不要指望本文件。

## 术语

**deck = 一份完整的幻灯片 = 一个 deck 项目。** 这三个说法指的是同一个东西：

| 说法 | 指的是 |
|---|---|
| **一个 deck** | 一整套幻灯片，对应磁盘上一个 `<工作目录>/slide/` 目录 |
| **一页 slide** | deck 里的一页，对应一个 `pages/NN-name.tex` 文件 |
| **记忆** | 模板与风格的定义，住在 `.slide-forge/` 里 |

**`\Deck*` 里的 `Deck` 只是命名前缀**（页面级命令用 `\Deck*`），与 deck 概念无关。

---

## 核心流程（先拿模板，再改项目）

**每一页都走这一条，新建和修改都一样。顺序不能反。**

```
① 读本地记忆 <deck>/slide/.slide-forge/templates/，找有没有现成的版式
   ├─ 命中一个 ──→ 直接用它，报一句"用 T3 排"
   ├─ 命中多个 ──→ 问用户，把候选摆出来让他选
   └─ 都没有 ────→ ②
② 在本地记忆里锻造新模板（唯一"造模板"的地方，不用问授权）
③ 模板拿到手之后，才写这一页的内容
```

**"先拿模板，再改项目"** = 即使是"把这页改一下"，也不是直接改页面文件；
先判断这个要求属于哪种版式 → 走①~③ → 拿到模板 → 再填内容。

---

## 三个记忆目录，三种权限

| 目录 | 是什么 | SKILL 能写吗 | 进 git |
|---|---|---|---|
| `~/.dsh/skills/slide-forge/base/` | **内置记忆**：出厂设置 | ❌ **不写**（用户也不能改） | 是 |
| `~/.dsh/skills/slide-forge/evolved/` | **仓库**：吸收来的 | ⚠️ 只在用户明确说"吸收"或他选同意时 | 否 |
| `<deck>/slide/.slide-forge/` | **本地记忆**：这个 deck 的一份 | ✅ 自动写，不用问授权 | 否 |

**三个目录的内部组织完全一致**——都是 `styles/` 与 `templates/`。
每个目录项都是一个自包含文件，所以复制粘贴就是"把文件塞进目录"。

### 为什么这么分

**本地记忆落在工作目录里**，写它不需要任何额外授权。所以"发现新版式 → 立刻
固化成模板"这一步可以**直接做**，不用中断去问"我要写工作区外面的文件夹，
可以吗"。要跨 deck 复用时，才由用户决定吸收。

### 两条不能搞反的规则

1. **编译只读本地记忆。** `main.tex` 只 `\input{.slide-forge/config.tex}`，
   一行都不指向 skill 目录。全局目录只在两个时刻被读：
   **新建 deck 时复制**、**吸收时写入**。
   —— 这一条是踩过坑才定的：曾把仓库垫在每份 deck 底下自动加载，结果任何
   一次吸收都会**静默改掉所有已有 deck 的排版**（实测零错误零警告，版式却变了）。
2. **吸收是逐字复制，不手改。** 仓库里那份必须与本地那份**内容完全一致**
   （只加一行来源标注）。两个目录结构相同、内容相同，只是位置不同 ——
   这样"同名不同内容"的冲突根本不会出现。

### 记忆里不许有任何非记忆内容

这三个目录**随时可能被删空**，所以：

- **里面只放定义与配置**：风格 token、模板定义、容量值
- **不要放 README、不要写"本目录是干嘛的""怎么用这个 skill"** ——
  一旦被删空那些说明就白写了，而且会让人以为"这目录里有东西"
- **不要放具体内容**（某个 deck 的标题、姓名、数据）—— 那是 deck 的事
- 机制、约定、流程**全部写在本文件与 `references/` 里**
- **没有内容时不要建文件**：文件在第一次有内容时才出现

**自检**：删掉任何一个记忆目录，skill 本身必须完好无损——
只是没有对应的风格或版式可选而已。

---

## 第一部分：加载链（决定放哪儿才算数）

```
<deck>/slide/main.tex
 └─ \input{.slide-forge/config.tex}            加载顺序在这里定
     ├─ \input{.slide-forge/primitives.tex}       ① 工具箱（无依赖）
     ├─ \input{.slide-forge/styles/X.tex}         ② 风格：值 + 容量（只依赖①）
     └─ 逐个 \IfFileExists 加载 templates/         ③ 版式（只依赖②）
```

**一条直线，每一步只依赖紧邻的上一层**：`primitives → styles → templates`。

- `primitives.tex` **无依赖** —— 它只**引用** `\DeckRowSkip` 这类名字，
  定义时并不求值
- `styles/X.tex` **只依赖 ①** —— 它不只定义值，末尾还**用那些值量出容量**
- `templates/` **只依赖 ②** —— 只用原语搭版式，**不反过来依赖别的模板**

**为什么"量容量"在风格文件里、而不是单独一个文件**：几何要用当前字体量字宽，
**没有字体量不出来**，所以尺子天生坐在参数后面。拆成单独文件就会变成
"`primitives` 与 `styles` 两个根汇合到 `geometry`"的**菱形依赖**。
放进风格文件末尾，依赖才是一条链。

代价是那段测量代码每个风格文件各有一份 —— **这是刻意的取舍**：
加新风格时复制一个现成风格文件、只改前面的值，那段原样保留即可。
换来的是每个风格文件**自己能看懂全部**，而且依赖形状是直线。

**为什么原语在 `primitives.tex` 而不在某个模板里**：`\DeckLine` 这类原语被
多个模板调用。放进某个模板会变成"所有模板都依赖它"；放进工具箱则依赖是
单向的。

**cwd 必须是 `<deck>/slide/`**——`config.tex` 里的路径都带 `.slide-forge/`
前缀，而 TeX 的相对路径按**当前工作目录**解析，不是按文件位置。

## 第二部分：每个文件只有一个变动原因

| 文件 | 装什么 | 什么时候变 |
|---|---|---|
| `.slide-forge/primitives.tex` | 画一页的工具（原语） | **永不变**（框架能力变了才变） |
| `.slide-forge/styles/*.tex` | 一套风格的**值 + 由值量出的容量** | 改长相时 |
| `.slide-forge/templates/*.tex` | 一个版式的结构 | 加/改版式时 |
| `.slide-forge/config.tex` | 选哪套风格 + 加载顺序 | 换风格时改**一行** |
| `main.tex` | 页面清单 | 加页时 |

**配置与工具的区别**：`config.tex` 是**配置**——可以丢掉重新写一份
（每个 deck 一份，值不同）；`primitives.tex` 是**工具**——扔了就没有框架了。
混在一起就叫耦合。`styles/` 里那个"量"的部分也属于工具性质，
但它是**用值算出来的量**，所以必须跟值住在一起。

**改排版时想清楚改哪个文件**：

- 换颜色/字号/间距 → `styles/` 里那一个文件
- 改某个版式的结构 → `templates/` 里那一个文件
- 加一个新版式 → `templates/` 里加一个新文件
- 换整套长相 → `config.tex` 里那一行

## 第三部分：为这一页选模板

### 什么时候直接选、什么时候问

- **直接选**：只有一个结构上说得通的候选。选完**报一句**用了哪个
  （"这一页用 T4 标签—长文字表排"），让用户有机会拦
- **必须问**：命中多个候选，或用户描述含糊分不清。问法要具体：

> 这一页可以有三种排法：
> ① **T3 名称—数值表**：名称在左、数值右对齐，适合成绩/指标；
> ② **T4 标签—长文字表**：窄标签在左、长文字占满，适合奖项/事件；
> ③ **T9 时间轴**（本 deck 里锻造的）：竖向节点轴。
> 你要哪种？

### 怎么发现有哪些模板可选

**读 `templates/` 下每个文件的头部注释。** 每个模板文件都写清了：
适用场景、依赖、参数、容量、易错点、以及一个"填满的样子"。
不需要任何索引文件——模板自己会说明自己。

### 三处记忆怎么查、怎么"拿到"

| 记忆 | 怎么查 | 怎么拿到 |
|---|---|---|
| **本地**（日常只看这里） | 读 `<deck>/slide/.slide-forge/templates/` | 本来就在，直接用 |
| **仓库**（只在用户要求时看） | 读 `~/.dsh/skills/slide-forge/evolved/templates/` | **复制**一份进本地记忆（逐字，不改） |
| **内置**（不必查） | `base/templates/` 里的模板 | 新建 deck 时已整份复制到本地，直接用 |

**日常操作只看本地记忆。** 只有在用户明确要求、或本地确实没有而他选用了
仓库里某一份时，才去读仓库。

## 第四部分：锻造新模板（自动进化）

### 触发条件

- 页面的结构无法用现有模板表达（需要新的分栏、新的对齐逻辑）
- 用户明确说"这页换个排法""做成时间线的样子"，而现有模板都不合适

### 流程

1. **在 `.slide-forge/templates/` 里新建一个文件**，文件名 `tNN-<名字>.tex`
   - **这一步直接做，不要问用户要授权** —— 它在工作目录内
   - 用 **`\DeckDefine`** 定义命令，**不要用 `\newcommand`** ——
     前者新建与覆盖都行，后者遇到已存在的命令会报 `already defined`
   - 命名沿用现有前缀：页面级用 `\Deck*`
   - **不要改 `base/`**，**不要直接写 `evolved/`**（那是吸收的落点）
2. **在同一个文件头部写全六项**（照抄现有模板的格式）：
   - 适用场景 —— 什么内容该用这个版式
   - 提供 —— 定义了什么命令
   - 依赖 —— 用到哪些原语或风格 token
   - 参数 —— 每个参数的范围与限制
   - 容量 —— 一页装几条、一行装几字（**必须实测，不要猜**，见第八部分）
   - 易错点 —— 已知的坑
   - **填满的样子** —— 一个用中性占位内容填满的调用示例
     （中性指"第一条""内容写在这里"，不要用具体项目的内容）
3. **在 `config.tex` 里加一行 `\IfFileExists` 加载它**
   —— 漏了这一步模板不会生效，而且不会报错（因为用的是 `\IfFileExists`）
4. **编译并渲染成图目视**（见第八部分）
5. **告诉用户一句，并提吸收**：
   "这页的时间轴排法现有模板装不下，我已经在本地记忆里锻造成 T9 了
   （组件 `\DeckTimeline`）。要吸收进全局仓库、让别的 deck 也能用吗？"
   - 用户说不要 → 到此为止，**不再追问**。
     但**下次别的东西再复用到它时，主动再提一次** —— 复用是它值得吸收的证据

## 第五部分：吸收到全局仓库

### 什么时候提

- 刚在本地记忆里锻造完一个新模板时（一次就好，问过就不再问）
- **复用节点**：某个本地模板被第二个 deck、或同一个 deck 的第二页用到时
- 用户自己说"吸收""把这个存进全局""以后别的 deck 也要用"

### 流程

1. **列出可吸收项**：读 `.slide-forge/templates/` 与 `.slide-forge/styles/`
   里有哪些文件是**本地独有的**（内置那份不算，它本来就在 `base/` 里）
2. **先做通用性检查**（吸收前唯一的关卡，别跳）
   - 内容里有没有**具体 deck 的东西**：产品名、专属数据、写死的年份人名
   - 有没有指向这个 deck 本地文件的路径
   - 是不是只在**这一页**用过、看不出会有第二次
   - 命中任何一条 → 报告给用户，建议不吸收
3. **查冲突**：`evolved/` 里同名文件已存在且内容不同 →
   报告差异，问用户是覆盖、另起名字、还是放弃
4. **逐字复制到 `~/.dsh/skills/slide-forge/evolved/` 对应目录**
   - `templates/t9-timeline.tex` → `evolved/templates/t9-timeline.tex`
   - `styles/dark.tex` → `evolved/styles/dark.tex`
   - **唯一的改动是加一行来源标注**：`% 吸收自 <deck 名>，<日期>`
   - ❌ **不要顺手改内容、不要"优化一下"、不要改参数** ——
     吸收是**搬运**，不是编辑。内容一改，仓库那份就和本地分叉了
   - 文件不存在就新建；**只放定义，不要写说明文字**
5. **验收：两份正文逐字一致**
6. **告诉用户**：复述一遍做了什么、**从现在起别的 deck 取用时拿到的是这一版**、
   以及本地那份**一字未动**

### 吸收之后

- **不必重编译任何 deck**：吸收只是往仓库里放了一份，没有 deck 会因此改变
  —— 仓库不在编译链上，这是它的设计前提
- 如果有哪个 deck **想**用这一版：把仓库那份复制进它的
  `.slide-forge/templates/`，再重编译目视

## 第六部分：经验与 SKILL 管理

### 用户可以自由增删改的，只有这两处

- `<deck>/slide/.slide-forge/` 里的任何东西
- `~/.dsh/skills/slide-forge/evolved/` 里的任何东西

移动、合并、改名、删除都行。

### 其余一律不改

`base/`、`SKILL.md`、`references/`：**任何操作都不写这些文件。**

**内置记忆 `base/` 是出厂设置**——用户没有修改权限，SKILL 也没有。
想改内置模板的排法，**正确做法是在本地记忆里做一个变体**：
把 `base/templates/t04-table-list.tex` 复制到 `.slide-forge/templates/`
再改。同名文件在本地记忆里会覆盖内置那份，而 `base/` 一个字没动。

**用户想改格式配置**（"换个颜色""字号大一点""表格再紧一点"）：
改 `.slide-forge/styles/` 里当前用的那一份。改完**必须重新实测容量并更新
注释里的数字**（见第八部分），然后提议吸收到仓库。

**判断顺序**：先动留白 → 再动颜色 → 最后才考虑字号。字号是最后手段，
因为它是唯一能让整页节奏崩掉的东西。

### 删除模板

1. 确定它在哪一层（内置 / 仓库 / 本地）
2. **先搜还有哪些页面在用它** —— 搜命令名，别只看文件名
3. 有页面在用 → 先问用户这些页改成哪种排法，**改完页面再删模板**
4. 删掉模板文件，并从 `config.tex` 里删掉对应的 `\IfFileExists` 那一行
5. **内置模板不删** —— 那不属于用户的经验。用户说"以后别用这种排法"时，
   在本地记忆里放一个同名文件覆盖它即可

## 第七部分：换风格

```
① 看 .slide-forge/styles/ 里有哪些风格文件
② 改 .slide-forge/config.tex 里 \SlideActiveStyle 那一行
③ 编译并逐页目视
```

**加一套新风格**：在 `styles/` 里加一个自包含文件。它必须定义其他风格也
定义的**同一组名字**（颜色、字体、字号、版心、垂直节奏、尺寸）——
缺一个就会在编译时报 `Undefined control sequence`，**响亮地失败**，
不会静默出错稿。

**改完风格必须重新实测容量**：字号一变，"一行几个字、一页几行"就变了。
把实测值写进该风格文件的注释与容量值里，别让文档和实际对不上。

## 第八部分：必须目视 + 必须实测

### 容量要实测，不要写死也不要猜

容量分两类，**混在一起是错误来源**：

| 类别 | 是什么 | 住哪 |
|---|---|---|
| **几何硬上限** | 物理上最多塞多少，超了报 overfull 并静默裁行 | 风格文件末尾的实测段 |
| **审美上限** | "看起来还舒服"的推荐值（留了余量） | `styles/` 里，是设计判断 |

**两个数不一样是正常的，不是矛盾。** 模板注释里两个都要写清：
推荐值给用户看，硬上限让人知道余量还有多少。

**改完风格或模板后，必须实测一次**：把内容一路加到一个已知超限的量，
确认 `Overfull \vbox` 出现的位置与注释里写的硬上限吻合。不吻合就更新注释。

### 必须渲染成图目视

beamer 的 frame 高度是硬的：**超出部分被静默裁掉，编译只有一条 overfull
警告，有时连警告都没有。** 所以新增或修改任何一页后都要：

```powershell
# cwd 必须是 <deck>/slide/
Remove-Item main.log -Force -ErrorAction SilentlyContinue   # ← 见下方警告
xelatex -interaction=nonstopmode main.tex
Select-String -Path main.log -Pattern "^! |Overfull|Font Warning"
pdftocairo -png -r 130 -f <页码> -l <页码> main.pdf out
```

然后**看图**，确认最后一行还在。

⚠ **读日志前必须先删日志。** 失败的 xelatex 常常根本不写 `main.log`，
上一次成功那份会原样留着 —— 你会读到过期证据，得出"编译成功"的相反结论。

### 更多排版陷阱

见 `references/typography.md`：cwd 陷阱、`\vspace` 为什么要带星号、
图片为什么要先缩放、链接为什么要用 pdftohtml 验证……

## 第九部分：文件布局

```
~/.dsh/skills/slide-forge/           全局 skill 目录
├── SKILL.md                         本文件：只有机制                [追踪]
├── .gitignore                       追踪边界                        [追踪]
├── references/typography.md         排版知识（人读）                [追踪]
│
├── base/                  【记忆①】内置：出厂设置，谁都不能改      [追踪]
│   ├── primitives.tex               工具箱：画一页的工具
│   ├── config.tex                   出厂配置 + 加载顺序
│   ├── main.tex                     页面清单骨架
│   ├── styles/default.tex           风格：值 + 量出的容量
│   └── templates/t00-core.tex 等
│
└── evolved/               【记忆②】仓库：吸收来的，只在用户同意时写 [忽略]
    ├── styles/
    └── templates/
```

```
<工作目录>/slide/                    一份 deck
├── main.tex                         页面清单
├── pages/                           内容，一页一个文件
├── figures/
├── main.pdf                         交付物
└── .slide-forge/         【记忆③】本地：这个 deck 的一份
    ├── primitives.tex               工具箱
    ├── config.tex                   选风格 + 加载顺序
    ├── styles/
    └── templates/
```

**没有索引文件、没有登记表。** 每个模板/风格文件自己说明自己。
仓库里只放"别人拿到就能直接用"的东西 —— `_verify/`、`evolved/` 都不追踪。

## 第十部分：新建 deck

用户说"新建一个 PPT / 做一份 slides"时：

1. **建目录并复制内置记忆**（只做一次）：
   ```
   <工作目录>/slide/
   ├── main.tex                ← 复制 base/main.tex（路径指向 .slide-forge/）
   ├── figures/                ← 空目录
   ├── pages/                  ← 空目录，内容由用户提供
   └── .slide-forge/           ← 复制 base/ 的 primitives.tex、config.tex、
                                  styles/、templates/ 全部
   ```
2. **把仓库里用户要用的模块复制进来**（可选）
   —— 只复制用户点名要的，不要整份搬运；`base/` 里已有的不用复制
3. 问用户：姓名、单位/身份、这份 deck 的用途（决定封面怎么写）
4. **建封面与致谢页**：按 `templates/t08-cover.tex` 与 `t99-thanks.tex`
   头部注释里的"填满的样子"写 `pages/00-cover.tex` 与 `pages/99-thanks.tex`
5. 正文页按用户给的内容逐页建（走核心流程：先选模板，再填内容）
6. **不要生成 `.gitignore`**，也不要建议用户给 deck 上 git ——
   那是他工作流的事，skill 不替他决定
7. 编译验证：零错误、零 overfull、零字体警告，并渲染封面目视

**初始化之后，这个 deck 再也不看全局目录**（除非用户要求吸收或取用）。
多个 deck 并行因此不会互相干扰 —— 每个 deck 有自己那份记忆。

## 第十一部分：反模式

- ❌ 在页面文件里写 `\fontsize` / `\vspace` / `\setbeamercolor` ——
  页面里只允许组件名 + 纯文本
- ❌ 在记忆目录里放 README、说明文件、索引、具体内容
- ❌ 为了"省事"把新模板直接写进 `evolved/`，绕开用户的选择权
- ❌ 吸收时顺手改内容（吸收是逐字搬运）
- ❌ 把 `evolved/` 写进编译链（那会让吸收静默改掉所有已有 deck 的排版）
- ❌ 改 `base/`——想改内置排法就在本地记忆里做同名变体
- ❌ 把容量数字写进 `SKILL.md` 或猜一个（必须实测，并写在记忆里）
- ❌ 用 `\newcommand` 在记忆文件里定义组件（覆盖时会报 already defined）
- ❌ 用 `\emph` / `\textit`（中文没有真斜体）
- ❌ 用圆点/方块做列表符号、用分隔线/小标题/色块分组——**用留白**
- ❌ 一页塞两组无关内容来"省页数"
- ❌ 只看编译通过就交付，不渲染成图目视
- ❌ 读 `main.log` 前不删旧日志（会读到过期证据，结论完全反过来）
- ❌ **遇到新版式却不固化成模板**（这是本 skill 存在的意义）
