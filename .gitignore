# ============================================================
# slide-forge 的追踪边界
#
# 目标：**仓库里只放"别人拿到就能直接用"的东西。**
#
#   base/        内置基线：模板、组件、格式配置、参考文件
#                —— 别人 clone 下来立刻能用，也是唯一必须存在的一层
#   SKILL.md     机制与规则
#   references/  排版知识与实测数据
#
#   evolved/     吸收来的经验 —— **本机专属**，别人不该拿到我的偏好
#   _verify/     开发用的验证脚本 —— 只有维护 skill 的人才需要
# ============================================================

# 本机专属：每个人吸收的经验不一样
evolved/

# 开发工具，不属于 skill 本体
_verify/

# 编译中间产物（万一有人在仓库里直接编译）
*.aux
*.log
*.nav
*.out
*.snm
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk

# 编辑器与系统杂物
.DS_Store
Thumbs.db
*~
*.swp
